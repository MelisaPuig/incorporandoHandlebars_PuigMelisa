extends layouts/main.pug
block append content
  h1 Datos del producto
  p
    a(class="link", href="/agregar")
      p Agregar productos
  if hayProductos
    table(class="tabla_productos", border="solid")
      tr
        th Título
        th Precio 
        th Thumbnail
      each producto in productos
        tr
          td #{producto.title}
          td $#{producto.price}
          td 
            img(src=producto.thumbnail, alt=producto.title, style="height: auto; width: 100px;")
  else
    div(class="no_hay_productos") No hay productos.